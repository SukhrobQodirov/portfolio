let options = {
    strings: [
        'Sukhrob Qodirov',
        'front end Developer',
        '18 years old'
    ],
    typeSpeed: 50,
    startDelay: 500,
    loop: true,
    backSpeed: 40,
    backDelay: 500,
}

let typed = new Typed('.element', options);